import React from 'react';
import LoaderImg from '../theme/loader.gif';

const Loader = () => (
	<img src={ LoaderImg } style={{display: "block",margin: "0 auto"}} height="70" width="70" alt="Loader"/>
);

export default Loader;
